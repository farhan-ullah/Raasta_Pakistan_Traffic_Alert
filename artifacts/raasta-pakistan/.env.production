# Deployed API (same host as SPA; nginx serves `/` and proxies `/api`).
# Vite injects this into the client bundle on `pnpm run build` (mode production).
VITE_PUBLIC_API_ORIGIN=http://5.189.173.244:8090
